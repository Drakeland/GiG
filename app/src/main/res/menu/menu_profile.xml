<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools">
    <item
        android:id="@+id/edit_profile_menu"
        android:title="@string/edit_profile"
        android:icon="@drawable/ic_mode_edit_white"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/upload_photo_menu"
        android:title="@string/upload_photo"
        android:icon="@drawable/ic_insert_photo"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/upload_music_menu"
        android:title="@string/upload_music"
        android:icon="@drawable/ic_action_music"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/sign_out_menu"
        android:title="@string/sign_out"
        app:showAsAction="never" />
</menu>